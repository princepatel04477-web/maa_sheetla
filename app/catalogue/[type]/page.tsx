import Link from 'next/link';
export async function generateStaticParams(){return ['sarees','lehengas','suits','garments'].map(type=>({type}));}
export async function generateMetadata({params}:{params:Promise<{type:string}>}){const {type}=await params; return {title:`${type[0].toUpperCase()+type.slice(1)} wholesale collection`,description:`Explore ${type} from Maa Sheetla Agency, Surat. Wholesale textile collections for boutiques across India.`};}
export default async function TypePage({params}:{params:Promise<{type:string}>}){const {type}=await params; const name=type[0].toUpperCase()+type.slice(1);return <main className="wrap page"><p className="eyebrow">CATALOGUE</p><h1 className="display">{name}</h1><p className="muted">TODO: product records, images, fabric, work type and MOQ pending client supply.</p><div className="tile" style={{minHeight:300}}><h3>Product catalogue</h3><small>Wholesale pricing available on WhatsApp.</small><Link className="btn" href="/partner">Enquire as a buyer →</Link></div></main>}
